---
description: "为子系统创建工作区并启动 Agent"
allowed-tools: [Bash]
---

Run the start-subsystem script with the subsystem name:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/cops-start-subsystem.sh" "$ARGUMENTS"
```
